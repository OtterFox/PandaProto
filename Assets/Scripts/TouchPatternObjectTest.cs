using UnityEngine;
using System.Collections;

public class TouchPatternObjectTest : MonoBehaviour {

	

	void Start ()
    {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}

    public void SingleTap()
    {
        //if(tPat == TestTouchPattern.Tap )   //REMEBER to put timer in there for misses
        {
            //Instantiate
        }
        //else player does nothing = miss instantiate
    }
}
